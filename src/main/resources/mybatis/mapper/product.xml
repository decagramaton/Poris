<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="poris.fruitlight.dao.ProductDao">
   <select id="SelectAll" resultType="poris.fruitlight.dto.ProductList">
      SELECT DISTINCT *
         FROM PRODUCT p, PRODUCT_BOARD b, BOARD_MEDIA m
         WHERE p.PRODUCT_NO = b.PRODUCT_NO AND b.BOARD_NO = m.BOARD_NO
   </select>
   
   <select id="SelectByPno" resultType="poris.fruitlight.dto.ProductList">
      SELECT *
         FROM PRODUCT p, PRODUCT_BOARD b, BOARD_MEDIA m
         WHERE p.PRODUCT_NO = b.PRODUCT_NO AND b.BOARD_NO = m.BOARD_NO AND (p.PRODUCT_NO IN (4,9,12))
   </select>

</mapper>